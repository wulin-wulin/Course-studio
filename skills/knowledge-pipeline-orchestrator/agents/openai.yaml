interface:
  display_name: "v2 知识流水线编排器"
  short_description: "通过固定工具编排内容、动画、图谱与两次结构化审核门禁"
  default_prompt: "使用 $knowledge-pipeline-orchestrator 和 course_pipeline 固定 action 生成课程；在 G2 知识点审核与 G6 知识图谱审核返回 pending 时停止，审核通过后再继续发布。"
